# Glossary — <package or repo name>
<!-- The ubiquitous language for THIS context. Read whole when entering this domain; no index.
     One line per term. No frontmatter — definitions are fiat, not facts.

     Rules (adapted from grill-with-docs):
     - Glossary ONLY. No implementation details, no decisions (those are cards). Define what a
       term IS, not what it does.
     - Scope filter: include a term ONLY if it is unique to this context. General programming
       concepts (timeout, retry, DTO, mutex…) do not belong, even if used heavily. The one
       question: is this unique to this repo, or knowledge the agent already has?
     - Be opinionated. When several words mean the same thing here, pick one and list the rest
       under `_Avoid:_` so the team (and the agent) converge on it.
     - Keep it tight — one or two sentences. Cross-link sibling terms in **bold**. -->

**Term** — tight, opinionated definition (what it IS, in this repo). _Avoid:_ near-synonyms that
mean the same thing here, so they don't fragment the language.

**Another term** — definition, optionally referencing a **Term** above. _Avoid:_ misleading near-synonyms.

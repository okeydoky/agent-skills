# Glossary — apps/web
<!-- Project-specific vocabulary only. One line per term. -->

**Live order updates** — the realtime status feed shown on an in-progress order. Delivered by
polling, not push. _Avoid:_ "order stream", "order socket" (we do not use sockets here).
